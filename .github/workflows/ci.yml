name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: ['1.21', '1.22', '1.23', '1.24']

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v6
      with:
        go-version: ${{ matrix.go-version }}

    - name: Cache Go modules
      uses: actions/cache@v4
      with:
        path: ~/go/pkg/mod
        key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-

    - name: Download dependencies
      run: go mod download

    - name: Verify dependencies
      run: go mod verify

    - name: Run tests
      run: go test -v -race -coverprofile=coverage.txt -covermode=atomic ./...

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v4
      with:
        file: ./coverage.txt
        flags: unittests
        name: codecov-umbrella

  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v6
      with:
        go-version: '1.24'

    - name: golangci-lint
      uses: golangci/golangci-lint-action@v4
      with:
        version: latest
        args: --timeout=5m

  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v6
      with:
        go-version: '1.24'

    - name: Build binaries
      run: |
        go build -v -o hyperexport ./cmd/hyperexport
        go build -v -o hypervisord ./cmd/hypervisord
        go build -v -o hyperctl ./cmd/hyperctl

    - name: Verify binaries
      run: |
        ./hypervisord --version
        ./hyperctl --version
        file hyperexport hypervisord hyperctl

    - name: Upload binaries
      uses: actions/upload-artifact@v7
      with:
        name: binaries
        path: |
          hyperexport
          hypervisord
          hyperctl

  build-rpm:
    name: Build RPM
    runs-on: ubuntu-latest
    container: fedora:latest

    steps:
    - name: Install dependencies
      run: |
        dnf install -y rpm-build rpmdevtools golang git

    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up RPM build tree
      run: |
        rpmdev-setuptree
        mkdir -p ~/rpmbuild/SOURCES

    - name: Create source tarball
      run: |
        VERSION=$(grep '^Version:' hypersdk.spec | awk '{print $2}')
        mkdir -p hypersdk-${VERSION}
        cp -r * hypersdk-${VERSION}/ || true
        tar czf ~/rpmbuild/SOURCES/hypersdk-${VERSION}.tar.gz hypersdk-${VERSION}

    - name: Build RPM
      run: |
        rpmbuild -ba hypersdk.spec

    - name: Upload RPM
      uses: actions/upload-artifact@v7
      with:
        name: rpm-packages
        path: |
          ~/rpmbuild/RPMS/**/*.rpm
          ~/rpmbuild/SRPMS/*.rpm

  integration-test:
    name: Integration Test
    runs-on: ubuntu-latest
    needs: build

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Download binaries
      uses: actions/download-artifact@v7
      with:
        name: binaries

    - name: Make binaries executable
      run: chmod +x hyperexport hypervisord hyperctl

    - name: Test daemon start
      run: |
        # Test daemon starts with default config
        timeout 5 ./hypervisord --version || true

    - name: Test hyperctl
      run: |
        # Start daemon in background
        ./hypervisord --addr localhost:9999 > /tmp/daemon.log 2>&1 &
        DAEMON_PID=$!
        sleep 3

        # Test status endpoint
        ./hyperctl --daemon http://localhost:9999 status || true

        # Clean up
        kill $DAEMON_PID || true

  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Run Gosec Security Scanner
      uses: securego/gosec@master
      with:
        args: ./...

    - name: Run Trivy vulnerability scanner
      uses: aquasecurity/trivy-action@master
      with:
        scan-type: 'fs'
        scan-ref: '.'
        format: 'sarif'
        output: 'trivy-results.sarif'

    - name: Upload Trivy results to GitHub Security tab
      uses: github/codeql-action/upload-sarif@v3
      with:
        sarif_file: 'trivy-results.sarif'
