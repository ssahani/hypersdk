# Archived live CDP rounds

One-off Node CDP scripts from the Aug 2026 deployed-host bug hunt. Kept for
history and selective re-runs. Prefer `../page-sweep.js` and `../api-sweep.js`.

These still hardcode host/credentials in places; patch env or edit before use.
WebSocket dependency is `ws` (install from parent: `cd .. && npm install`).
