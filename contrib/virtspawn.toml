# virtspawn daemon configuration
# Installed to /etc/virtspawn/config.toml
#
# User config at ~/.virtspawn/config.toml takes precedence.
# CLI arguments override all config file values.

[general]
refresh_interval_secs = 5

[daemon]
host = "127.0.0.1"
port = 8081

[libvirt]
uri = "qemu:///system"
